تمام، بما أنك **في مرحلة تعديل على مشروع موجود**، أهم شيء في البرومبت أن نجبر النموذج على التعامل مع المطلوب كـ **إضافة Features فقط**، وعدم إعادة بناء أو تغيير أي شيء سابق.

# ⚠️ تعديل على مشروع موجود — إضافة Features فقط

أنت تعمل الآن على **مشروع لعبة مداقش موجود ومكتمل جزئيًا**.

## القاعدة الأهم قبل أي تعديل

**لا تقم بإعادة بناء المشروع، ولا تعيد كتابة الأجزاء الموجودة، ولا تغير أي ميزة أو منطق أو UI يعمل حاليًا إلا إذا كان التعديل ضروريًا جدًا لربط الميزة الجديدة.**

أريد منك تنفيذ **الإضافات التالية فقط** فوق المشروع الحالي.

### ممنوع:

* إعادة تصميم المشروع بالكامل.
* تغيير الـUI الموجود بدون حاجة.
* تغيير نظام توزيع الأوراق الحالي.
* تغيير نظام الرصيد الحالي.
* تغيير قواعد قوة الأوراق الحالية.
* تغيير نظام اللاعبين والغرفة.
* حذف أي Feature موجودة.
* إعادة كتابة ملفات كاملة لمجرد إضافة الميزة.
* تغيير سلوك أي شيء لم أطلب تغييره.

### المطلوب:

**افحص المشروع الحالي أولًا، افهم الـArchitecture والـGame State الحالي، ثم أضف الميزات التالية بأقل تغييرات ممكنة.**

---

# 1. إضافة مرحلة جديدة: مرحلة المفاوضة

حاليًا بعد أن ينتهي جميع اللاعبين من تحديد مبلغ الدخول، **لا تنتهي الجولة ولا تبدأ مقارنة الأوراق مباشرة.**

أضف مرحلة جديدة:

```text
BETTING
↓
NEGOTIATION
↓
SHOWDOWN
↓
RESULT
```

بعد اكتمال دخول اللاعبين تبدأ:

# 🤝 مرحلة المفاوضة

---

# 2. تحديد "الكبير"

بعد انتهاء مرحلة الدخول، حدد اللاعب صاحب:

**أعلى مبلغ دخول.**

مثال:

```text
Player A = 5000
Player B = 2000
Player C = 500
Player D = 1500
```

إذن:

```text
الكبير = Player A
أعلى سومه = 5000
```

لا تغير أي شيء في نظام تحديد المبالغ الموجود حاليًا.

استخدم البيانات الموجودة في Game State.

---

# 3. ترتيب المفاوضة

المفاوضة تبدأ من **يمين الكبير** وتستمر بشكل دائري.

مثال:

```text
                الكبير
              Player A
                 ↓
        Player D ← Player B
                 ↓
              Player C
```

إذا كان Player B هو الموجود مباشرة على يمين الكبير، يبدأ التفاوض معه.

يجب أن يكون ترتيب المفاوضة محسوبًا اعتمادًا على **موقع اللاعبين/الموزع الموجود أصلًا في المشروع** وليس ترتيب دخولهم للغرفة.

---

# 4. من يفاوض من؟

الكبير هو صاحب أعلى مبلغ دخل.

هو الذي يفاوض اللاعبين الآخرين الذين دخلوا بمبالغ أقل.

مثال:

```text
الكبير = 5000

Player B = 500
Player C = 2000
Player D = 1500
```

الكبير يبدأ من اللاعب الموجود على يمينه.

مثلاً:

```text
الكبير → Player B
```

ثم:

```text
الكبير → Player C
```

ثم:

```text
الكبير → Player D
```

---

# 5. واجهة المفاوضة

عندما يأتي دور لاعب معين، تظهر له رسالة واضحة:

```text
🤝 مفاوضة

فلان يقول لك:

"خذ 1000 واطلع"

[ قبول العرض ]
[ لا ورق ]
```

مثال:

إذا الكبير دخل بـ:

```text
5000
```

وفاوض لاعبًا دخل بـ:

```text
500
```

يمكن أن يقول له:

```text
خذ 1000 واطلع
```

ويظهر للاعب:

```text
فلان يعرض عليك:

1,000

مقابل الخروج من الجولة.

[ قبول 1,000 ]
[ لا ورق ]
```

---

# 6. معنى "قبول العرض"

إذا اختار اللاعب:

**قبول العرض**

فهو يوافق على عرض الكبير ويخرج من المنافسة.

يجب تسجيل العملية في Game State.

مثال:

```text
Player A:
Offer → Player B
Amount → 1000
Response → ACCEPT
```

ويظهر للاعب أنه خرج من الجولة بناءً على اتفاق المفاوضة.

يجب أن يتم تطبيق نظام الرصيد/التسوية وفق المنطق المالي الموجود حاليًا في المشروع، ولا تخترع نظام خصم أو تحويل جديدًا إذا كان المشروع يحتوي أصلًا على نظام تسوية.

إذا لم يكن هناك نظام واضح للتسوية، افصل **قيمة العرض** كـ Negotiation Transaction حتى يمكن ربطها بنظام النتائج الحالي بدون كسر الرصيد.

---

# 7. معنى "لا ورق"

إذا رفض اللاعب العرض وضغط:

**لا ورق**

فهذا يعني:

> "أرفض العرض، وسأساومك بأوراقي."

أي أن اللاعب يتحدى الكبير بأن:

**ورقي أقوى من ورقك.**

عند اختيار:

```text
لا ورق
```

لا يتم كشف الأوراق مباشرة.

ولا يتم تحديد الفائز في هذه اللحظة.

يتم تسجيل:

```text
Response = NO_CARDS
```

وتستمر المفاوضة.

---

# 8. ظهور المفاوضات لجميع اللاعبين

هذه نقطة مهمة جدًا.

**كل اللاعبين يجب أن يروا سجل المفاوضات لحظيًا.**

مثلاً:

```text
🤝 سجل المفاوضات

الكبير → أحمد
"خذ 1000 واطلع"

أحمد → الكبير
"لا ورق"

الكبير → خالد
"خذ 1500 واطلع"

خالد → الكبير
"قبلت"
```

لا تجعل المفاوضة ظاهرة فقط للشخص الذي يتم التفاوض معه.

يجب أن يتم عرض سجل المفاوضات لكل لاعب من جهته (Perspective-based Log)، بحيث يظهر له من زاويته هو وكأنه جزء من واجهة لعبه الشخصية، وليس كسجل مركزي مستقل أو جدول موحد.

مع ذلك، تبقى جميع الأحداث متزامنة ومرئية لبقية اللاعبين، لكن طريقة العرض تختلف حسب منظور كل لاعب.

---

# 9. التفاوض لا يوقف باقي اللاعبين

أريد تسريع اللعبة.

بما أن ترتيب المفاوضة معروف مسبقًا، **لا تجعل اللاعب الذي لم يصل دوره ينتظر حتى يصل دوره لكي يختار.**

مثال:

ترتيب المفاوضة:

```text
1. أحمد
2. خالد
3. محمد
```

الكبير يفاوض أحمد حاليًا.

لكن خالد ومحمد يستطيعان مسبقًا تحديد ردودهما إذا كان ذلك ممكنًا حسب حالة اللعبة.

---

# 10. Pre-Selection

أضف نظام:

```text
PRESELECT RESPONSE
```

اللاعب الذي لم يصل دوره يستطيع اختيار موقفه مسبقًا.

مثلاً خالد يختار:

```text
[ سأقبل ]
```

أو:

```text
[ لا ورق ]
```

لكن هذا الاختيار **لا يتم تنفيذه مباشرة**.

بل يتم حفظه:

```text
pendingResponse
```

وعندما يصل الدور إلى خالد:

إذا كان اختياره المسبق صالحًا، يتم تطبيقه تلقائيًا ويظهر في سجل المفاوضة.

مثال:

```text
خالد اختار مسبقًا:

لا ورق

...

وصل دوره

خالد → الكبير
"لا ورق"
```

وهذا فقط لتسريع اللعبة.

---

# 11. عرض المبلغ

الكبير يستطيع تحديد قيمة العرض لكل لاعب.

مثلاً:

```text
أعلى سومه:
5000
```

يمكنه عرض:

```text
500
1000
1500
2000
2500
3000
3500
4000
4500
5000
```

لكن يجب تطبيق القواعد التالية.

---

# 12. الحد الأدنى للعرض

العرض يجب أن يكون:

**500 على الأقل.**

ويجب أن يكون من مضاعفات 500.

صحيح:

```text
500
1000
1500
2000
...
```

خطأ:

```text
200
750
1250
```

---

# 13. الحد الأعلى للعرض

العرض لا يمكن أن يتجاوز:

**أعلى سومه دخل بها الكبير.**

مثال:

الكبير:

```text
5000
```

إذن أعلى عرض:

```text
5000
```

ولا يمكن:

```text
5500
```

---

# 14. الكبير يستطيع تقسيم سومته

هذه قاعدة مهمة جدًا.

الكبير لديه مثلاً:

```text
5000
```

ولا يشترط أن يعطي كل لاعب نفس المبلغ.

مثلاً:

```text
Player B → 1000
Player C → 2000
Player D → 2000
```

المجموع:

```text
1000 + 2000 + 2000 = 5000
```

وبالتالي تم توزيع كامل سومته.

---

# 15. إذا استُهلك مبلغ الكبير

إذا أعطى الكبير لاعبًا عرضًا بقيمة:

```text
5000
```

فإن المبلغ المتبقي من سومته يصبح:

```text
0
```

وبالتالي بقية اللاعبين الذين لم يتم عرض مبلغ لهم يصبح موقفهم تلقائيًا:

```text
لا ورق
```

لأن الكبير لم يعد لديه مبلغ متاح لتقديم عرض لهم.

مثال:

```text
الكبير = 5000

أحمد → عرض 5000
```

ثم:

```text
المتبقي = 0
```

لذلك:

```text
خالد → تلقائيًا لا ورق
محمد → تلقائيًا لا ورق
```

ويظهر ذلك في سجل المفاوضة بشكل واضح.

---

# 16. لا تسمح بتجاوز رصيد الكبير المخصص للمفاوضة

أنشئ قيمة واضحة:

```text
negotiationBudget
```

مثلاً:

```text
negotiationBudget = 5000
```

كل عرض يتم خصمه من المبلغ المتاح.

مثال:

```text
5000

عرض أحمد = 1000
المتبقي = 4000

عرض خالد = 2000
المتبقي = 2000

عرض محمد = 2000
المتبقي = 0
```

لا يمكن إنشاء عرض يتجاوز:

```text
remainingNegotiationBudget
```

---

# 17. ماذا يحدث عند "لا ورق"؟

إذا قال اللاعب:

```text
لا ورق
```

فلا يتم كشف الأوراق في تلك اللحظة.

يتم وضع اللاعب في حالة:

```text
CHALLENGE
```

مثلاً:

```text
أحمد
الحالة: لا ورق
```

ويتم حسم قوة أوراقه لاحقًا في مرحلة:

```text
SHOWDOWN
```

---

# 18. نهاية مرحلة المفاوضة

تنتهي المفاوضة عندما:

* يتم التعامل مع جميع اللاعبين الذين يمكن مفاوضتهم.
* أو يستهلك الكبير كامل مبلغ المفاوضة.
* أو تنتهي جميع الخيارات المسبقة.
* أو لا يبقى لاعب يحتاج إلى قرار.

ثم تنتقل اللعبة إلى:

```text
SHOWDOWN
```

---

# 19. مرحلة كشف الأوراق

بعد انتهاء المفاوضة:

يتم تحديد المراكز بناءً على:

* قوة اليد.
* نتائج المفاوضات.
* اللاعبين الذين قبلوا العرض.
* اللاعبين الذين قالوا "لا ورق".

لا تكشف الأوراق قبل الوقت المناسب.

---

# 20. شاشة نهاية الجولة

بعد تحديد المراكز، أضف شاشة نتائج مختصرة وواضحة.

مثال:

```text
🏆 نتائج الجولة

🥇 نواف
🥈 أحمد
🥉 خالد
4️⃣ محمد
```

وبجانب كل لاعب:

```text
نواف
A A K Q

أحمد
K K Q J

خالد
A A A J

محمد
Q Q J J
```

---

# 21. عرض الأوراق بشكل مرتب ومختصر

لا تجعل أوراق اللاعبين تأخذ مساحة كبيرة.

استخدم Cards صغيرة أو عرضًا أفقيًا:

```text
نواف
[A][A][K][Q]

أحمد
[K][K][Q][J]

خالد
[A][A][A][J]

محمد
[Q][Q][J][J]
```

مع اسم نوع اليد:

```text
نواف
[A][A][K][Q]
ثنائي آكه
```

وهكذا.

---

# 22. سجل المفاوضات في النتيجة

يمكن إظهار ملخص صغير:

```text
🤝 المفاوضات

نواف → أحمد
عرض: 1000
أحمد: لا ورق

نواف → خالد
عرض: 1500
خالد: قبل

نواف → محمد
لا يوجد رصيد متبقي
محمد: لا ورق
```

---

# 23. Game State

أضف حالات واضحة إلى الـGame Engine الحالي دون كسر الـState الموجود.

مثلاً:

```text
NEGOTIATION
```

مع بيانات:

```typescript
negotiation: {
  active: boolean,

  bigPlayerId: string,

  order: string[],

  currentIndex: number,

  highestEntry: number,

  remainingBudget: number,

  offers: [],

  responses: {},

  pendingResponses: {},

  log: []
}
```

**لا تستبدل Game State الحالي بالكامل.**

ادمج هذه البيانات مع الـState الموجود.

---

# 24. Server Authority

جميع قرارات المفاوضة المهمة يجب أن يتم التحقق منها على السيرفر.

السيرفر هو المسؤول عن:

* تحديد الكبير.
* تحديد ترتيب المفاوضة.
* التحقق من قيمة العرض.
* التحقق من مضاعفات 500.
* التحقق من الحد الأدنى 500.
* التحقق من الحد الأعلى.
* حساب remainingBudget.
* تسجيل قبول العرض.
* تسجيل لا ورق.
* تطبيق الاختيار المسبق.
* الانتقال إلى SHOWDOWN.
* تحديد النتائج.

لا تثق بالـClient.

---

# 25. لا تكسر المشروع الحالي

مرة أخرى:

**هذه إضافة Feature وليست إعادة بناء.**

قبل كتابة أي كود:

1. اقرأ الـArchitecture الحالي.
2. حدد Game State الموجود.
3. حدد مراحل اللعبة الموجودة.
4. حدد نظام الرصيد الحالي.
5. حدد نظام الموزع الحالي.
6. حدد طريقة حساب الفائز الحالية.
7. حدد نظام Multiplayer الحالي.

ثم أضف:

```text
NEGOTIATION
```

كمرحلة جديدة بين:

```text
BETTING
```

و:

```text
SHOWDOWN
```

مع الحفاظ على كل ما سبق كما هو.

إذا كان هناك Function موجودة تؤدي وظيفة قريبة من المطلوب، **أعد استخدامها بدل إنشاء نسخة جديدة منها.**

إذا كان تعديل ملف موجود ضروريًا، قم بأقل تعديل ممكن.

لا تحذف كودًا يعمل إلا إذا كان هناك تعارض حقيقي.

---

# 26. Acceptance Criteria

اعتبر المهمة مكتملة فقط إذا كان السيناريو التالي يعمل:

```text
4 لاعبين
↓
توزيع الأوراق
↓
كل لاعب يحدد مبلغ الدخول
↓
500 / 1000 / 1500 / ...
↓
أعلى مبلغ = الكبير
↓
بدء NEGOTIATION
↓
المفاوضة تبدأ من يمين الكبير
↓
الكبير يقدم عرضًا
↓
اللاعب يرى العرض
↓
يستطيع قبول العرض أو اختيار لا ورق
↓
كل اللاعبين يشاهدون سجل المفاوضة
↓
اللاعبون القادمون يستطيعون Pre-select
↓
عند وصول دورهم يتم تنفيذ اختيارهم
↓
الكبير يستطيع تقسيم مبلغه
↓
لا يتجاوز Budget
↓
إذا وصل Budget إلى 0
↓
البقية يصبحون لا ورق تلقائيًا
↓
انتهاء المفاوضة
↓
SHOWDOWN
↓
تحديد المراكز
↓
عرض أوراق جميع اللاعبين بشكل مختصر
↓
عرض نوع اليد
↓
تسوية الجولة
↓
الجولة التالية
```

## مهم جدًا

**لا تعدل أي شيء آخر في اللعبة غير المطلوب في هذه الوثيقة.**

إذا احتجت تعديل Component أو Function موجودة لربط مرحلة المفاوضة، عدّلها بأقل قدر ممكن.

بعد التنفيذ، أعطني ملخصًا قصيرًا فقط عن:

1. الملفات التي تم تعديلها.
2. الملفات الجديدة.
3. الـGame State الذي تمت إضافته.
4. طريقة انتقال اللعبة إلى NEGOTIATION.
5. طريقة عمل Pre-selection.
6. طريقة حساب remainingBudget.

لا تعيد شرح المشروع كاملًا ولا تعيد بناء أجزاء لم أطلب تعديلها.
